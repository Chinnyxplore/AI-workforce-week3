Microsoft copilot is an AI assistant builton microsoft 365application. there are different AI assistant out there but copilot belongs to Microsoft.
It basically help users create, summarize and analyze work using natural language.
It is grounded in Microsft365 Data, which works base on the data that one has in their microsoft365 tenant.
if there is no data for copilot to tap into it will give generic responses when it's being asked questions.
For a userto have copipot they must have the right license.
It follows the existing Microsoft365 security by tapping into your data.
It allows access only to data one has access to.
It relies heavily on the structure of microsoft365.
On windows 11 by default copilot chat is seen and used.
Copilot is as good as the Microsoft365 behind it.
If there is no strong Identity management copilot cannot function clean in the Microsoft365 environment.
